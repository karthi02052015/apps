<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="color-scheme" content="dark">

<title>Subnet & IP Calculator</title>

<style>
* {
    box-sizing: border-box;
}

html {
    background: #070b12 !important;
}

body {
    margin: 0;
    min-height: 100vh;
    background:
        radial-gradient(circle at top left, #101b2c 0, #070b12 35%),
        #070b12 !important;
    color: #e8eef7;
    font-family:
        Inter,
        system-ui,
        -apple-system,
        BlinkMacSystemFont,
        "Segoe UI",
        sans-serif;
}

button,
input,
select {
    font: inherit;
    color-scheme: dark;
}

.app {
    width: min(1400px, calc(100% - 30px));
    margin: auto;
    padding: 28px 0 30px;
}

/* HEADER */

.header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 20px;
    margin-bottom: 22px;
}

.header h1 {
    margin: 0;
    font-size: clamp(26px, 4vw, 40px);
    letter-spacing: -1.5px;
    color: #fff;
}

.header p {
    margin: 7px 0 0;
    color: #8491a5;
    font-size: 14px;
}

.status {
    padding: 7px 11px;
    border-radius: 999px;
    background: #0b1827;
    border: 1px solid #244565;
    color: #7fc4ff;
    font-family: monospace;
    font-size: 11px;
    white-space: nowrap;
}

/* CARD */

.card {
    background:
        linear-gradient(145deg, #101722, #0b111a) !important;
    border: 1px solid #202b3b !important;
    border-radius: 16px;
    box-shadow: 0 18px 55px rgba(0,0,0,.35);
}

/* INPUT */

.input-card {
    padding: 20px;
    margin-bottom: 15px;
}

.label-title {
    color: #7f8da2;
    font-size: 11px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: .13em;
    margin-bottom: 13px;
}

.ip-input-area {
    display: flex;
    align-items: center;
    gap: 9px;
}

.octet {
    width: 100%;
    min-width: 0;
    text-align: center;

    padding: 14px 8px;

    background: #080d15 !important;
    border: 1px solid #2a374a !important;
    border-radius: 10px;

    color: #f1f5f9 !important;

    font-family:
        ui-monospace,
        SFMono-Regular,
        Menlo,
        Consolas,
        monospace;

    font-size: 17px;
    font-weight: 600;

    outline: none;

    transition: .15s;
}

.octet:focus {
    border-color: #3b82f6 !important;
    box-shadow: 0 0 0 3px rgba(59,130,246,.13);
}

.dot {
    color: #526176;
    font-family: monospace;
    font-size: 22px;
}

.slash {
    color: #526176;
    font-family: monospace;
    font-size: 22px;
}

.prefix-select {
    width: 230px;

    padding: 14px;

    background: #080d15 !important;
    color: #f1f5f9 !important;

    border: 1px solid #2a374a !important;
    border-radius: 10px;

    outline: none;

    cursor: pointer;
}

.prefix-select:focus {
    border-color: #3b82f6 !important;
    box-shadow: 0 0 0 3px rgba(59,130,246,.13);
}

.error {
    min-height: 20px;
    margin-top: 8px;
    color: #fb7185;
    font-family: monospace;
    font-size: 11px;
}

.examples {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
    margin-top: 2px;
}

.example {
    padding: 6px 9px;

    border-radius: 7px;

    background: #0a1019;
    border: 1px solid #263449;

    color: #8997aa;

    font-family: monospace;
    font-size: 10px;

    cursor: pointer;
}

.example:hover {
    color: white;
    border-color: #3d5875;
}

/* GRID */

.grid {
    display: grid;
    grid-template-columns: repeat(12, minmax(0,1fr));
    gap: 14px;
}

.col-6 {
    grid-column: span 6;
}

.col-12 {
    grid-column: span 12;
}

/* RESULT */

.result-card {
    padding: 18px;
    min-width: 0;
}

.card-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;

    margin-bottom: 12px;
}

.card-header h2 {
    margin: 0;
    font-size: 14px;
}

.card-header span {
    color: #58667a;
    font-family: monospace;
    font-size: 10px;
}

.value-row {
    min-height: 43px;

    display: flex;
    align-items: center;
    justify-content: space-between;

    gap: 12px;

    border-bottom: 1px solid #1b2533;
}

.value-row:last-child {
    border-bottom: 0;
}

.value-label {
    color: #7d8ba0;
    font-size: 12px;
}

.value-container {
    display: flex;
    align-items: center;
    gap: 7px;
    min-width: 0;
}

.value-container code {
    color: #dce8f8;

    font-family: monospace;
    font-size: 12px;

    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.copy {
    flex-shrink: 0;

    padding: 5px 7px;

    background: #090f17;
    border: 1px solid #2a384c;

    color: #738197;

    border-radius: 6px;

    font-family: monospace;
    font-size: 9px;

    cursor: pointer;
}

.copy:hover {
    color: white;
    border-color: #48617f;
}

.copy.copied {
    color: #34d399;
    border-color: #23614e;
}

/* CAPACITY */

.capacity {
    display: grid;
    grid-template-columns: repeat(3,1fr);
    gap: 9px;
    margin-bottom: 10px;
}

.metric {
    padding: 12px;

    background: #090f17 !important;
    border: 1px solid #1f2b3b;

    border-radius: 10px;
}

.metric-number {
    color: #edf5ff;
    font-family: monospace;
    font-size: 16px;
    font-weight: 600;
    overflow-wrap: anywhere;
}

.metric-label {
    margin-top: 4px;
    color: #647286;
    font-size: 9px;
    text-transform: uppercase;
    letter-spacing: .08em;
}

/* CLASSIFICATION */

.classification {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
    margin-bottom: 6px;
}

.badge {
    padding: 7px 9px;
    border-radius: 7px;
    border: 1px solid;
    font-family: monospace;
    font-size: 10px;
    font-weight: 700;
}

.public {
    color: #93c5fd;
    background: #091728;
    border-color: #234d7b;
}

.private {
    color: #6ee7b7;
    background: #091c17;
    border-color: #205b49;
}

.loopback {
    color: #c4b5fd;
    background: #130d20;
    border-color: #4d3c78;
}

.linklocal {
    color: #fcd34d;
    background: #1b1607;
    border-color: #69531c;
}

.special {
    color: #fda4af;
    background: #1d0b10;
    border-color: #6b2734;
}

/* BINARY */

.binary-card {
    padding: 20px;
}

.binary-description {
    color: #718096;
    font-size: 11px;
    margin: -4px 0 18px;
}

.binary-section {
    margin-bottom: 20px;
}

.binary-heading {
    display: flex;
    justify-content: space-between;
    gap: 10px;
    margin-bottom: 8px;
}

.binary-heading strong {
    color: #cbd5e1;
    font-size: 12px;
}

.binary-heading span {
    color: #647287;
    font-family: monospace;
    font-size: 10px;
}

.bits {
    display: grid;
    grid-template-columns: repeat(32,minmax(0,1fr));
    gap: 2px;
}

.bit {
    min-width: 0;
    text-align: center;

    padding: 8px 0;

    border-radius: 4px;

    font-family: monospace;
    font-size: 11px;

    border: 1px solid;
}

.ip-network {
    background: #102b46;
    color: #70b9ff;
    border-color: #214e76;
}

.ip-host {
    background: #151b25;
    color: #9ba8ba;
    border-color: #202b3a;
}

.mask-network {
    background: #12382f;
    color: #6ee7b7;
    border-color: #24604f;
}

.mask-host {
    background: #17181d;
    color: #7f8794;
    border-color: #292b31;
}

.octet-labels {
    display: grid;
    grid-template-columns: repeat(4,1fr);
    gap: 2px;
    margin-top: 4px;
}

.octet-labels div {
    text-align: center;
    color: #4f5d70;
    font-family: monospace;
    font-size: 9px;
}

.legend {
    display: flex;
    flex-wrap: wrap;
    gap: 15px;
    margin-top: 15px;
}

.legend-item {
    display: flex;
    align-items: center;
    gap: 6px;

    color: #68768a;
    font-size: 10px;
}

.legend-square {
    width: 10px;
    height: 10px;
    border-radius: 3px;
}

.legend-network {
    background: #214e76;
}

.legend-host {
    background: #202b3a;
}

.legend-mask {
    background: #24604f;
}

.footer {
    padding-top: 20px;
    text-align: center;
    color: #465366;
    font-family: monospace;
    font-size: 10px;
}

/* RESPONSIVE */

@media(max-width:900px) {

    .col-6 {
        grid-column: span 12;
    }

}

@media(max-width:650px) {

    .app {
        width: calc(100% - 20px);
        padding-top: 18px;
    }

    .header {
        flex-direction: column;
    }

    .ip-input-area {
        gap: 5px;
    }

    .octet {
        padding: 13px 3px;
        font-size: 15px;
    }

    .dot,
    .slash {
        font-size: 18px;
    }

    .prefix-select {
        width: 100%;
        margin-top: 10px;
    }

    .ip-input-area {
        flex-wrap: wrap;
    }

    .octet {
        flex: 1 1 18%;
    }

    .slash {
        display: none;
    }

    .capacity {
        grid-template-columns: 1fr;
    }

    .result-card,
    .binary-card,
    .input-card {
        padding: 14px;
    }

    .bit {
        padding: 6px 0;
        font-size: 8px;
    }

}

@media(max-width:420px) {

    .bits {
        gap: 1px;
    }

    .bit {
        font-size: 7px;
        padding: 5px 0;
    }

}
</style>
</head>

<body>

<div class="app">

<header class="header">

    <div>
        <h1>Subnet & IP Calculator</h1>

        <p>
            IPv4 network boundaries, host allocation,
            masks and binary visualization.
        </p>
    </div>

    <div class="status">
        ● OFFLINE • IPv4
    </div>

</header>


<!-- =====================================================
     INPUT
===================================================== -->

<section class="card input-card">

    <div class="label-title">
        IPv4 Address & Subnet
    </div>

    <div class="ip-input-area">

        <input
            id="octet1"
            class="octet"
            inputmode="numeric"
            maxlength="3"
            value="192"
            aria-label="IPv4 first octet"
        >

        <span class="dot">.</span>

        <input
            id="octet2"
            class="octet"
            inputmode="numeric"
            maxlength="3"
            value="168"
            aria-label="IPv4 second octet"
        >

        <span class="dot">.</span>

        <input
            id="octet3"
            class="octet"
            inputmode="numeric"
            maxlength="3"
            value="1"
            aria-label="IPv4 third octet"
        >

        <span class="dot">.</span>

        <input
            id="octet4"
            class="octet"
            inputmode="numeric"
            maxlength="3"
            value="45"
            aria-label="IPv4 fourth octet"
        >

        <span class="slash">/</span>

        <select
            id="prefix"
            class="prefix-select">
        </select>

    </div>

    <div
        id="error"
        class="error">
    </div>


    <div class="examples">

        <button
            class="example"
            data-ip="192.168.1.45"
            data-prefix="24">
            192.168.1.45/24
        </button>

        <button
            class="example"
            data-ip="10.20.30.40"
            data-prefix="16">
            10.20.30.40/16
        </button>

        <button
            class="example"
            data-ip="172.16.5.10"
            data-prefix="20">
            172.16.5.10/20
        </button>

        <button
            class="example"
            data-ip="8.8.8.8"
            data-prefix="32">
            8.8.8.8/32
        </button>

    </div>

</section>


<!-- =====================================================
     RESULTS
===================================================== -->

<main class="grid">


<!-- NETWORK SUMMARY -->

<section class="card result-card col-6">

    <div class="card-header">
        <h2>Network Summary</h2>
        <span>BOUNDARIES</span>
    </div>

    <div class="value-row">
        <span class="value-label">Input IP</span>

        <div class="value-container">
            <code id="outIp">—</code>
            <button class="copy" data-copy="outIp">COPY</button>
        </div>
    </div>

    <div class="value-row">
        <span class="value-label">Network ID</span>

        <div class="value-container">
            <code id="outNetwork">—</code>
            <button class="copy" data-copy="outNetwork">COPY</button>
        </div>
    </div>

    <div class="value-row">
        <span class="value-label">Broadcast</span>

        <div class="value-container">
            <code id="outBroadcast">—</code>
            <button class="copy" data-copy="outBroadcast">COPY</button>
        </div>
    </div>

    <div class="value-row">
        <span class="value-label">Subnet Mask</span>

        <div class="value-container">
            <code id="outMask">—</code>
            <button class="copy" data-copy="outMask">COPY</button>
        </div>
    </div>

    <div class="value-row">
        <span class="value-label">Wildcard Mask</span>

        <div class="value-container">
            <code id="outWildcard">—</code>
            <button class="copy" data-copy="outWildcard">COPY</button>
        </div>
    </div>

    <div class="value-row">
        <span class="value-label">CIDR Network</span>

        <div class="value-container">
            <code id="outCidr">—</code>
            <button class="copy" data-copy="outCidr">COPY</button>
        </div>
    </div>

</section>


<!-- HOST ALLOCATION -->

<section class="card result-card col-6">

    <div class="card-header">
        <h2>Host Allocation</h2>
        <span>HOST SPACE</span>
    </div>

    <div class="capacity">

        <div class="metric">
            <div
                id="totalAddresses"
                class="metric-number">
                —
            </div>

            <div class="metric-label">
                Total Addresses
            </div>
        </div>

        <div class="metric">
            <div
                id="usableHosts"
                class="metric-number">
                —
            </div>

            <div class="metric-label">
                Usable Hosts
            </div>
        </div>

        <div class="metric">
            <div
                id="networkBits"
                class="metric-number">
                —
            </div>

            <div class="metric-label">
                Network Bits
            </div>
        </div>

    </div>

    <div class="value-row">
        <span class="value-label">First Usable</span>

        <div class="value-container">
            <code id="firstHost">—</code>
            <button class="copy" data-copy="firstHost">COPY</button>
        </div>
    </div>

    <div class="value-row">
        <span class="value-label">Last Usable</span>

        <div class="value-container">
            <code id="lastHost">—</code>
            <button class="copy" data-copy="lastHost">COPY</button>
        </div>
    </div>

</section>


<!-- CLASSIFICATION -->

<section class="card result-card col-6">

    <div class="card-header">
        <h2>IP Classification</h2>
        <span>ADDRESS TYPE</span>
    </div>

    <div
        id="classification"
        class="classification">
    </div>

    <div class="value-row">
        <span class="value-label">Address Type</span>

        <div class="value-container">
            <code id="addressType">—</code>
        </div>
    </div>

    <div class="value-row">
        <span class="value-label">Prefix</span>

        <div class="value-container">
            <code id="outPrefix">—</code>
        </div>
    </div>

</section>


<!-- DETAILS -->

<section class="card result-card col-6">

    <div class="card-header">
        <h2>Subnet Details</h2>
        <span>CALCULATION</span>
    </div>

    <div class="value-row">
        <span class="value-label">Host Bits</span>

        <div class="value-container">
            <code id="hostBits">—</code>
        </div>
    </div>

    <div class="value-row">
        <span class="value-label">Subnet Size</span>

        <div class="value-container">
            <code id="subnetSize">—</code>
        </div>
    </div>

    <div class="value-row">
        <span class="value-label">Address Offset</span>

        <div class="value-container">
            <code id="offset">—</code>
        </div>
    </div>

</section>


<!-- BINARY -->

<section class="card binary-card col-12">

    <div class="card-header">
        <h2>32-bit Binary Visualization</h2>
        <span>NETWORK / HOST</span>
    </div>

    <div class="binary-description">
        Network bits are highlighted in blue/green.
        Host bits are shown in gray.
    </div>


    <div class="binary-section">

        <div class="binary-heading">

            <strong>
                IP Address
            </strong>

            <span id="binaryIpText">
                —
            </span>

        </div>

        <div
            id="ipBits"
            class="bits">
        </div>

        <div class="octet-labels">
            <div>OCTET 1</div>
            <div>OCTET 2</div>
            <div>OCTET 3</div>
            <div>OCTET 4</div>
        </div>

    </div>


    <div class="binary-section">

        <div class="binary-heading">

            <strong>
                Subnet Mask
            </strong>

            <span id="binaryMaskText">
                —
            </span>

        </div>

        <div
            id="maskBits"
            class="bits">
        </div>

        <div class="octet-labels">
            <div>OCTET 1</div>
            <div>OCTET 2</div>
            <div>OCTET 3</div>
            <div>OCTET 4</div>
        </div>

    </div>


    <div class="legend">

        <div class="legend-item">
            <span class="legend-square legend-network"></span>
            Network bits
        </div>

        <div class="legend-item">
            <span class="legend-square legend-host"></span>
            Host bits
        </div>

        <div class="legend-item">
            <span class="legend-square legend-mask"></span>
            Mask network bits
        </div>

    </div>

</section>

</main>


<footer class="footer">
    Pure client-side IPv4 calculator • No server requests
</footer>

</div>


<script>

/* =========================================================
   ELEMENTS
========================================================= */

const octets = [
    document.getElementById("octet1"),
    document.getElementById("octet2"),
    document.getElementById("octet3"),
    document.getElementById("octet4")
];

const prefixSelect =
    document.getElementById("prefix");

const errorEl =
    document.getElementById("error");

const MAX32 = 0xFFFFFFFFn;


/* =========================================================
   PREFIX DROPDOWN
========================================================= */

function maskFromPrefix(prefix) {

    if (prefix === 0)
        return "0.0.0.0";

    const mask =
        (MAX32 << BigInt(32 - prefix))
        & MAX32;

    return uintToIp(mask);
}


for (let i = 0; i <= 32; i++) {

    const option =
        document.createElement("option");

    option.value = i;

    option.textContent =
        `/${i} — ${maskFromPrefix(i)}`;

    prefixSelect.appendChild(option);

}


/* =========================================================
   IP CONVERSION
========================================================= */

function ipToUint(ip) {

    const p =
        ip.split(".").map(Number);

    return (
        (BigInt(p[0]) << 24n) |
        (BigInt(p[1]) << 16n) |
        (BigInt(p[2]) << 8n) |
        BigInt(p[3])
    );
}


function uintToIp(value) {

    return [
        Number((value >> 24n) & 255n),
        Number((value >> 16n) & 255n),
        Number((value >> 8n) & 255n),
        Number(value & 255n)
    ].join(".");
}


function ipToBinary(ip) {

    return ip
        .split(".")
        .map(octet =>
            Number(octet)
                .toString(2)
                .padStart(8,"0")
        )
        .join("");

}


/* =========================================================
   VALIDATION
========================================================= */

function getIp() {

    return octets
        .map(input => input.value.trim())
        .join(".");
}


function validateIp() {

    for (let i = 0; i < 4; i++) {

        const value =
            octets[i].value.trim();

        if (value === "") {

            return `Octet ${i + 1} cannot be empty.`;

        }

        if (!/^\d{1,3}$/.test(value)) {

            return `Octet ${i + 1} must contain numbers only.`;

        }

        const number =
            Number(value);

        if (number < 0 || number > 255) {

            return `Octet ${i + 1} must be between 0 and 255.`;

        }

    }

    return null;

}


/* =========================================================
   CLASSIFICATION
========================================================= */

function range(ip, start, end) {

    const value = ipToUint(ip);

    return (
        value >= ipToUint(start) &&
        value <= ipToUint(end)
    );

}


function classifyIp(ip) {

    const result = [];


    /* RFC 1918 */

    if (
        range(ip,"10.0.0.0","10.255.255.255") ||
        range(ip,"172.16.0.0","172.31.255.255") ||
        range(ip,"192.168.0.0","192.168.255.255")
    ) {

        result.push({
            text: "PRIVATE",
            className: "private"
        });

    }


    /* LOOPBACK */

    if (
        range(ip,"127.0.0.0","127.255.255.255")
    ) {

        result.push({
            text: "LOOPBACK",
            className: "loopback"
        });

    }


    /* LINK LOCAL */

    if (
        range(ip,"169.254.0.0","169.254.255.255")
    ) {

        result.push({
            text: "LINK-LOCAL",
            className: "linklocal"
        });

    }


    /* MULTICAST */

    if (
        range(ip,"224.0.0.0","239.255.255.255")
    ) {

        result.push({
            text: "MULTICAST",
            className: "special"
        });

    }


    /* RESERVED / SPECIAL */

    if (
        range(ip,"0.0.0.0","0.255.255.255")
    ) {

        result.push({
            text: "SPECIAL",
            className: "special"
        });

    }


    if (result.length === 0) {

        result.push({
            text: "PUBLIC",
            className: "public"
        });

    }


    return result;

}


/* =========================================================
   SET TEXT
========================================================= */

function setText(id,value) {

    const element =
        document.getElementById(id);

    if (element)
        element.textContent = value;

}


/* =========================================================
   CLEAR RESULTS
========================================================= */

function clearResults() {

    const ids = [

        "outIp",
        "outNetwork",
        "outBroadcast",
        "outMask",
        "outWildcard",
        "outCidr",

        "totalAddresses",
        "usableHosts",
        "networkBits",

        "firstHost",
        "lastHost",

        "addressType",
        "outPrefix",

        "hostBits",
        "subnetSize",
        "offset",

        "binaryIpText",
        "binaryMaskText"

    ];

    ids.forEach(id =>
        setText(id,"—")
    );


    document.getElementById(
        "classification"
    ).innerHTML = "";

    document.getElementById(
        "ipBits"
    ).innerHTML = "";

    document.getElementById(
        "maskBits"
    ).innerHTML = "";

}


/* =========================================================
   BINARY RENDER
========================================================= */

function renderBinary(
    containerId,
    binary,
    prefix,
    mask
) {

    const container =
        document.getElementById(containerId);

    container.innerHTML = "";


    [...binary].forEach(
        (bit,index) => {

            const element =
                document.createElement("div");

            element.className = "bit";


            if (index < prefix) {

                element.classList.add(
                    mask
                    ? "mask-network"
                    : "ip-network"
                );

            } else {

                element.classList.add(
                    mask
                    ? "mask-host"
                    : "ip-host"
                );

            }


            element.textContent = bit;

            element.title =
                `Bit ${index + 1}: ${
                    index < prefix
                    ? "Network"
                    : "Host"
                }`;


            container.appendChild(element);

        }
    );

}


/* =========================================================
   MAIN CALCULATION
========================================================= */

function calculate() {

    const validation =
        validateIp();


    if (validation) {

        errorEl.textContent =
            validation;

        clearResults();

        return;

    }


    errorEl.textContent = "";


    const ip =
        getIp();

    const prefix =
        Number(prefixSelect.value);


    const ipValue =
        ipToUint(ip);


    /* MASK */

    const mask =
        prefix === 0
        ? 0n
        : (
            MAX32 <<
            BigInt(32 - prefix)
        ) & MAX32;


    const wildcard =
        MAX32 ^ mask;


    /* NETWORK */

    const network =
        ipValue & mask;


    /* BROADCAST */

    const broadcast =
        network | wildcard;


    /* HOST BITS */

    const hostBits =
        32 - prefix;


    /* TOTAL */

    const total =
        2n ** BigInt(hostBits);


    /* HOST RANGE */

    let usable;
    let first;
    let last;


    if (prefix === 32) {

        usable = 1n;
        first = ipValue;
        last = ipValue;

    }

    else if (prefix === 31) {

        /*
         * RFC 3021
         * Both addresses usable on
         * point-to-point networks.
         */

        usable = 2n;

        first = network;
        last = broadcast;

    }

    else {

        usable = total - 2n;

        first = network + 1n;
        last = broadcast - 1n;

    }


    /* OFFSET */

    const offset =
        ipValue - network;


    /* =====================================================
       OUTPUT
    ===================================================== */

    setText(
        "outIp",
        ip
    );

    setText(
        "outNetwork",
        uintToIp(network)
    );

    setText(
        "outBroadcast",
        uintToIp(broadcast)
    );

    setText(
        "outMask",
        uintToIp(mask)
    );

    setText(
        "outWildcard",
        uintToIp(wildcard)
    );

    setText(
        "outCidr",
        `${uintToIp(network)}/${prefix}`
    );


    setText(
        "totalAddresses",
        total.toLocaleString()
    );

    setText(
        "usableHosts",
        usable.toLocaleString()
    );

    setText(
        "networkBits",
        `${prefix}`
    );


    setText(
        "firstHost",
        uintToIp(first)
    );

    setText(
        "lastHost",
        uintToIp(last)
    );


    setText(
        "outPrefix",
        `/${prefix}`
    );

    setText(
        "hostBits",
        `${hostBits}`
    );

    setText(
        "subnetSize",
        total.toLocaleString()
    );

    setText(
        "offset",
        offset.toLocaleString()
    );


    /* =====================================================
       CLASSIFICATION
    ===================================================== */

    const classification =
        document.getElementById(
            "classification"
        );

    classification.innerHTML = "";


    const classes =
        classifyIp(ip);


    classes.forEach(item => {

        const badge =
            document.createElement("span");

        badge.className =
            `badge ${item.className}`;

        badge.textContent =
            item.text;

        classification.appendChild(
            badge
        );

    });


    setText(
        "addressType",
        classes
            .map(item => item.text)
            .join(" • ")
    );


    /* =====================================================
       BINARY
    ===================================================== */

    const ipBinary =
        ipToBinary(ip);

    const maskBinary =
        ipToBinary(
            uintToIp(mask)
        );


    setText(
        "binaryIpText",
        `${ip} → ${
            ipBinary
                .match(/.{8}/g)
                .join(".")
        }`
    );


    setText(
        "binaryMaskText",
        `${uintToIp(mask)} → ${
            maskBinary
                .match(/.{8}/g)
                .join(".")
        }`
    );


    renderBinary(
        "ipBits",
        ipBinary,
        prefix,
        false
    );


    renderBinary(
        "maskBits",
        maskBinary,
        prefix,
        true
    );

}


/* =========================================================
   OCTET INPUTS
========================================================= */

octets.forEach((input, index) => {

    input.addEventListener(
        "input",
        () => {

            /*
             * Only allow digits.
             */

            input.value =
                input.value
                    .replace(/\D/g,"")
                    .slice(0,3);


            /*
             * Automatically move to next octet
             * when 3 digits are entered.
             */

            if (
                input.value.length === 3 &&
                index < 3
            ) {

                octets[index + 1].focus();

                octets[index + 1].select();

            }


            calculate();

        }
    );


    /*
     * Keyboard navigation
     */

    input.addEventListener(
        "keydown",
        event => {

            if (
                event.key === "ArrowRight" &&
                input.selectionStart === input.value.length &&
                index < 3
            ) {

                octets[index + 1].focus();

            }


            if (
                event.key === "ArrowLeft" &&
                input.selectionStart === 0 &&
                index > 0
            ) {

                octets[index - 1].focus();

            }


            /*
             * Backspace on empty field moves backwards.
             */

            if (
                event.key === "Backspace" &&
                input.value === "" &&
                index > 0
            ) {

                octets[index - 1].focus();

            }

        }
    );

});


/* =========================================================
   PREFIX
========================================================= */

prefixSelect.addEventListener(
    "change",
    calculate
);


/* =========================================================
   EXAMPLE BUTTONS
========================================================= */

document.querySelectorAll(
    ".example"
).forEach(button => {

    button.addEventListener(
        "click",
        () => {

            const ip =
                button.dataset.ip;

            const parts =
                ip.split(".");


            parts.forEach(
                (part,index) => {

                    octets[index].value =
                        part;

                }
            );


            prefixSelect.value =
                button.dataset.prefix;


            calculate();

        }
    );

});


/* =========================================================
   COPY BUTTONS
========================================================= */

document.addEventListener(
    "click",
    async event => {

        const button =
            event.target.closest(".copy");

        if (!button)
            return;


        const target =
            document.getElementById(
                button.dataset.copy
            );


        if (!target)
            return;


        const text =
            target.textContent.trim();


        if (!text || text === "—")
            return;


        try {

            await navigator.clipboard.writeText(
                text
            );

        } catch {

            const textarea =
                document.createElement(
                    "textarea"
                );

            textarea.value = text;

            textarea.style.position =
                "fixed";

            textarea.style.opacity =
                "0";

            document.body.appendChild(
                textarea
            );

            textarea.select();

            document.execCommand(
                "copy"
            );

            textarea.remove();

        }


        const old =
            button.textContent;

        button.textContent =
            "COPIED";

        button.classList.add(
            "copied"
        );


        setTimeout(
            () => {

                button.textContent =
                    old;

                button.classList.remove(
                    "copied"
                );

            },
            1000
        );

    }
);


/* =========================================================
   INITIALIZE
========================================================= */

prefixSelect.value = "24";

calculate();

</script>

</body>
</html>
